import Slider from "../components/Slider";
const { results } = require("../../mocks/en-us/featured-banners.json");

const Home = () => {
  console.log("*****", Mock);
  return <Slider />;
};

export default Home;
